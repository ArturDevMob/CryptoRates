RateTopCoinsFragment - Выводит список криптовалют, полученных от вьюмодел или ошибки,
в зависимости от подключения к сети и наличия элементов в списке с курсами.

В RateTopCoinsFragmentTest есть 3 основных кейса для тестирования:

1. Есть подключение к сети, отобразить курсы валют полученные из сети
2. Нет подключения к сети, отобразить курсы валют получены из кэша,
    показать ошибку о разрыве подключения к сети и кнопку перезапуска подключения к сети
3. Нет подключения к сети и нет курсов для отображения (кэш пуст),
    показать ошибку о разрыве подключения к сети, ошибку об отсутствие кэшированных курсов
    и кнопку перезапуска подключения к сети.

Чтобы сделать такие тесты, нужно управлять данными, которые возвращает CoinRepository
Даггером (TestAppComponent и TestDataModule) подменил реализацию CoinRepository
с CoinRepositoryImpl на CoinRepositoryTest
И при запуске теста, использую своё правило для запуска активити DaggerActivityTestRule,
в котором AppComponent подменяется на TestAppComponent

Таким образом, получаю данные не из сети, а фейки.
В таком случае, только первый кейс тестов будет пройден успешно, 2 и 3 не выполнится,
т.к. нужно вызвать ошибку подключения к сети.

Для этого в CoinRepositoryTest передается ResourceCoinType с одним из 3 enum, которым указываем CoinRepositoryTest
какие данные вернуть: Из сети, из кеша, пустой список

ResourceCoinType передается в репозиторий через конструктор правила DaggerActivityTestRule(ResourceCoinType), а далее в модуль даггера
Затем в нужным нам тестах, мы перезапускаем активити с новыми правилом